const Follow = require('../models/follow');

// Controller that allows a user to follow another user:

async function createFollow(req, res, next) {
  // TO-DO: FINISH THIS CONTROLLER
}

// Controller that allows a user to un-follow another user:

async function deleteFollow(req, res, next) {
  // TO-DO: FINISH THIS CONTROLLER
}

module.exports = {
  createFollow,
  deleteFollow
};